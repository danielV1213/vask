{
  "name": "vask",
  "description": "Vask is a simple task tracker Web Application which was created to showcase some of the fundamentals from the Progressive JavaScript Framework Vue.js.",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vue-cli-service serve --open",
    "build": "vue-cli-service build",
    "deploy": "npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "core-js": "^3.8.3",
    "vue": "^3.2.13"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-service": "~5.0.0"
  }
}
